<?php

/**
 * Implements hook_theme().
 */
function dr_data_explorer_theme($existing, $type, $theme, $path) {
  return array(
    'dr_data_explorer' => array(
      'variables' => array(
        'title'=>'',
        'files_to_add'=>[],
        'dqparams'=>[],
        'options'=>[],
        'userconfig'=>[],
      ),
    )
  );
}
